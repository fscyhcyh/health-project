<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cyh.dao.CheckItemDao">
    <!--新增-->
    <insert id="save" parameterType="com.cyh.pojo.CheckItem">
        insert into t_checkitem(code,name,gender,age,price,type,remark,attention)
        values
        (#{code},#{name},#{gender},#{age},#{price},#{type},#{remark},#{attention})
    </insert>

    <select id="selectByCondition" parameterType="map" resultType="com.cyh.pojo.CheckItem">
        select * from t_checkitem
        <if test="queryString != null and queryString.length >0 ">
            where code like '%${queryString}%' or name like  '%${queryString}%'
        </if>
    </select>
<!--    concat('%',#{queryString},'%') or name like concat('%',#{queryString},'%')-->

    <select id="countByCheckItemId" parameterType="int" resultType="long">
        select count(*) from t_checkgroup_checkitem where checkitem_id = #{checkItemId}
    </select>

    <delete id="deleteById" parameterType="int">
        delete from t_checkitem where id = #{id}
    </delete>

    <select id="selectById" parameterType="int" resultType="com.cyh.pojo.CheckItem">
        select * from t_checkitem where id = #{id}
    </select>

    <select id="selectAll"  resultType="com.cyh.pojo.CheckItem">
        select * from t_checkitem
    </select>

    <update id="update" parameterType="com.cyh.pojo.CheckItem">
        update t_checkitem
        set name = #{name},gender = #{gender},code = #{code}, age = #{age},
            price = #{price},type = #{type},attention = #{attention},remark = #{remark}
        where id = #{id}
    </update>

    <select id="selectCheckItemByCheckGroupId" resultType="com.cyh.pojo.CheckItem">
        select * from t_checkitem where id in
        (select checkitem_id from t_checkgroup_checkitem where checkgroup_id = #{checkgroup_id})
    </select>

</mapper>