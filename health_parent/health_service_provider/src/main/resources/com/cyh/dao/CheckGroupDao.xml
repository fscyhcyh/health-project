<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cyh.dao.CheckGroupDao">
    
    <resultMap id="baseResultMap" type="com.cyh.pojo.CheckGroup" >
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="helpCode" property="helpCode"/>
        <result column="sex" property="sex"/>
        <result column="remark" property="remark"/>
        <result column="attention" property="attention"/>
    </resultMap>
    <resultMap id="selectCheckGroupBySetmealIdResultMap" type="com.cyh.pojo.CheckGroup" extends="baseResultMap">
        <collection property="checkItems"
                    javaType="ArrayList"
                    ofType="com.cyh.pojo.CheckItem"
                    select="com.cyh.dao.CheckItemDao.selectCheckItemByCheckGroupId"
                    column="id">
        </collection>
    </resultMap>
    
    
    <!--新增-->
    <insert id="save" parameterType="com.cyh.pojo.CheckGroup">
        <selectKey order="AFTER" resultType="Integer" keyProperty="id">
            select LAST_INSERT_ID()
        </selectKey>
        insert into t_checkgroup(code,name,gender,helpCode,remark,attention)
        values
        (#{code},#{name},#{gender},#{helpCode},#{remark},#{attention})
    </insert>

    <insert id="saveCheckGroupCheckItem" parameterType="map">
        insert into t_checkgroup_checkitem (checkgroup_id, checkitem_id)
        values (#{checkGroupId}, #{checkItemId})
    </insert>

    <select id="selectByCondition" parameterType="map" resultType="com.cyh.pojo.CheckGroup">
        select * from t_checkgroup
        <if test="queryString != null and queryString.length >0 ">
            where code like '%${queryString}%' or name like  '%${queryString}%'
        </if>
    </select>
<!--    concat('%',#{queryString},'%') or name like concat('%',#{queryString},'%')-->

<!--    <select id="countByCheckGroupId" parameterType="int" resultType="long">-->
<!--        select count(*) from t_checkgroup_checkitem where checkitem_id = #{checkItemId}-->
<!--    </select>-->

<!--    <delete id="deleteById" parameterType="int">-->
<!--        delete from t_checkitem where id = #{id}-->
<!--    </delete>-->

    <select id="selectById" parameterType="int" resultType="com.cyh.pojo.CheckGroup">
        select * from t_checkgroup where id = #{id}
    </select>

    <select id="selectItemsByGroupId" parameterType="int" resultType="Integer">
        select checkitem_id from t_checkgroup_checkitem
            where checkgroup_id = #{id}
    </select>

    <update id="update" parameterType="com.cyh.pojo.CheckGroup">
        update t_checkgroup
        set name = #{name},gender = #{gender},helpCode = #{helpCode},code = #{code},attention = #{attention},remark = #{remark}
        where id = #{id}
    </update>

    <delete id="deleteCheckGroupCheckItemsByGroupId" parameterType="int">
        delete from t_checkgroup_checkitem where checkgroup_id = #{checkGroupId}
    </delete>

    <select id="selectAll" resultType="com.cyh.pojo.CheckGroup">
        select * from t_checkgroup
    </select>

    <select id="selectCheckGroupBySetmealId" resultMap="selectCheckGroupBySetmealIdResultMap">
        select * from t_checkgroup where id in
        (select checkgroup_id from t_setmeal_checkgroup where setmeal_id = #{setmeal_id})
    </select>

</mapper>